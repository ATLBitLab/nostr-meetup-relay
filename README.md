To generate fake events for testing run `node generate_event.js` and copy paste the output in postman or whatever.

```
# Install deps
yarn

# Run dev server
yarn dev

# Build and run for production
yarn start
```
